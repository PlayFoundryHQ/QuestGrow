apiVersion: v2
name: questgrow
description: QuestGrow backend — FastAPI /v1 API + reference web clients
type: application
version: 0.1.0          # chart version — bump on chart changes
appVersion: "0.3.0"     # default backend image tag (override in values)
