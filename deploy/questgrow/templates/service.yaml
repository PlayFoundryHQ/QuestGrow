apiVersion: v1
kind: Service
metadata:
  name: {{ include "questgrow.name" . }}
  labels: {{- include "questgrow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "questgrow.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
