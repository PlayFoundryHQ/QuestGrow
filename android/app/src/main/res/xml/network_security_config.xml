<?xml version="1.0" encoding="utf-8"?>
<!--
  Release policy: HTTPS everywhere, except the local development backends
  (emulator host loopback / localhost). A production deployment talks TLS to a
  real QuestGrow backend. The debug build overrides this with a permissive
  config (src/debug/res/xml/) so QA can point the app at any host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
